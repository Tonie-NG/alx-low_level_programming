#include <stdio.h>
/**
 * main - A program that prints a string using printf
 * Return: 0 (return value of zero indicates success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
