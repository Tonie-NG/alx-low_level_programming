#include <stdio.h>
/**
 * main - Program to print the alphabet
 * Return: 0 (Indicates success)
 */
int main(void)
{
	char t;

	t = 'a';
	while
		(t <= 'z') {
			putchar(t);
			t++;
		}
	putchar('\n');
	return (0);
